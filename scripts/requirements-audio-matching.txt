# Audio Matching Script Dependencies
# Install with: pip install -r requirements-audio-matching.txt

# Audio processing and analysis (music-focused)
librosa>=0.10.0

# Scientific computing (required by librosa)
numpy>=1.24.0
scipy>=1.10.0

# Audio codec support (librosa dependencies)
soundfile>=0.12.0
audioread>=3.0.0

# HTTP requests for downloading audio
requests>=2.31.0

# Environment variables
python-dotenv>=1.0.0

# Convex client
convex>=0.5.0

# Note: No ffmpeg required! librosa handles audio loading natively.
# However, for some audio formats (MP3, M4A), you may optionally install:
#   - ffmpeg: brew install ffmpeg (macOS)
#   - audioread will use ffmpeg if available for better format support
